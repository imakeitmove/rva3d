import fs from 'node:fs/promises';const b=await fs.readFile('docs/editorial-v2/remote-file-tree.json'),tree=JSON.parse((b[0]===255?b.toString('utf16le'):b.toString('utf8')).replace(/^\uFEFF/,'')),flat=[];function walk(nodes,parent=''){for(const n of nodes){const path=parent+n.name;if(n.children)walk(n.children,path+'/');else flat.push({path,type:n.type,uid:n.uid});}}walk(tree);await fs.writeFile('docs/editorial-v2/remote-file-list.json',JSON.stringify(flat,null,2));
