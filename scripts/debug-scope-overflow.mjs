import {localBrowser,pause} from './candidate-browser.mjs';const b=await localBrowser();try{await b.send('Emulation.setDeviceMetricsOverride',{width:1920,height:1080,deviceScaleFactor:1,mobile:false});await b.send('Page.navigate',{url:'http://127.0.0.1:4321/review/site/'});await pause(900);console.log(await b.evaluate("[...document.querySelectorAll('body *')].filter(e=>{const r=e.getBoundingClientRect();return r.width>0&&(r.right>innerWidth+1||r.left< -1)&&getComputedStyle(e).position!=='absolute'}).map(e=>({tag:e.tagName,cls:e.className,txt:e.textContent.slice(0,60),r:e.getBoundingClientRect().right,l:e.getBoundingClientRect().left})).slice(0,20)"));}finally{await b.close();}
